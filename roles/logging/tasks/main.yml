---
# logging — Phase 7
# Journal retention, logrotate, and syslog forwarding.
#
# STATUS: NOT YET IMPLEMENTED.
#
# This role fails loudly rather than reporting success while doing nothing.
# A green Ansible run that changed nothing is the most expensive kind of bug in
# infrastructure code: you believe a machine is configured when it is not.
#
# Planned responsibilities:
#   - Cap journald size and retention
#   - Logrotate policies for nginx, gunicorn, and PostgreSQL
#   - Forward to a central collector (Loki, Graylog, or ELK) later
#   - Keep audit logs on a separate rotation from application logs
#
# To implement: fill defaults/main.yml first, then templates/, then these tasks,
# then delete the fail below and update the status table in docs/roles.md.

- name: Refuse to run — the logging role is not implemented yet
  ansible.builtin.fail:
    msg: >-
      The 'logging' role is a Phase 7 stub and has no tasks yet.
      Nothing was configured on {{ inventory_hostname }}.
      Implement it, or remove it from the playbook you are running.
      See docs/roles.md and roles/logging/README.md.
  when: not logging_enabled | bool
  tags:
    - logging
    - stub

# Implemented tasks go here, guarded by the same flag while you develop:
#
# - name: Install logging
#   ansible.builtin.apt:
#     name: "{{ logging_packages }}"
#     state: present
#   when: logging_enabled | bool
#   notify: Restart logging

---
galaxy_info:
  role_name: cloudflare
  namespace: jengasec
  author: Strathmore Cybersecurity Club
  description: Cloudflare Tunnel and Access, so the platform is reachable off campus.
  company: Strathmore University
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - bookworm
  galaxy_tags:
    - jengasec
    - infrastructure
    - cloudflare
    - tunnel

# nginx and certificates must have run — the tunnel forwards to nginx over TLS
# and verifies it against the internal CA. Ordered in playbooks/cloudflare.yml
# rather than declared here; the role asserts both at run time with messages
# that name the fix.
dependencies: []

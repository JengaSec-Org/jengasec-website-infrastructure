---
galaxy_info:
  role_name: django
  namespace: jengasec
  author: Strathmore Cybersecurity Club
  description: "Deploy the JengaSec platform — code, virtualenv, environment, migrations."
  company: Strathmore University
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - bookworm
  galaxy_tags:
    - jengasec
    - infrastructure
    - django
    - deployment

# `python` and `git` must have run, and PostgreSQL must be reachable. Both are
# ordered in playbooks/platform.yml rather than declared here — a meta
# dependency would re-run the python role every time this one is used, and the
# database is on a different host entirely, which meta dependencies cannot
# express. The role checks for both at run time instead.
dependencies: []

---
galaxy_info:
  role_name: gunicorn
  namespace: jengasec
  author: Strathmore Cybersecurity Club
  description: systemd socket and service units running Django under gunicorn.
  company: Strathmore University
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - bookworm
  galaxy_tags:
    - jengasec
    - infrastructure
    - gunicorn
    - django

# The gunicorn PACKAGE comes from the platform's requirements.txt, installed
# into the virtualenv by the `django` role. Ordering is enforced in
# playbooks/platform.yml, not declared here — this role owns the units, and
# should stay usable on its own for a units-only change.
dependencies: []

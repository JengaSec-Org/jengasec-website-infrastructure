---
galaxy_info:
  role_name: nginx
  namespace: jengasec
  author: Strathmore Cybersecurity Club
  description: "nginx as TLS terminator, load balancer, and static/media server."
  company: Strathmore University
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Debian
      versions:
        - bookworm
  galaxy_tags:
    - jengasec
    - infrastructure
    - nginx
    - loadbalancer

# `certificates` must have run — nginx will not start without the certificate it
# is configured to use. Ordered in playbooks/networking.yml and platform.yml
# rather than declared here, and the role asserts the file exists at run time
# with a message that names the fix.
dependencies: []
